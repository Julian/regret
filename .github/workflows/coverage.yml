name: Coverage

on:
  push:
  pull_request:
  release:
    types: [published]

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install tox
      run: python -m pip install tox
    - name: Collect & Upload Coverage
      run: python -m tox -e py310-codecov
      env:
        CODECOV_TOKEN: d65a4e2e-9035-4bab-946d-d0b78744e5b4
